# ChatForge
ChatForge is a lightweight, embeddable AI chatbot platform built for local and small businesses. Powered by LangChain and any OpenAI-compatible API, it delivers streaming responses, per-session memory, and a smart fallback engine — all configurable via a single `.env` file and deployable with one `&lt;script>` tag.
